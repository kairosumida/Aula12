@model IEnumerable<Aula12.Models.StatusUsuario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StatusUsuarioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StatusUsuarioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StatusUsuarioId })
        </td>
    </tr>
}

</table>
